export default () => {
    /**
     * This function is called when the pointer is missed
     */
    const onFeaturesPointerMissed = (event: MouseEvent) => {};

    return {
        onFeaturesPointerMissed,
    };
};
