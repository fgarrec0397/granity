import { Triplet } from "@react-three/cannon";

export default {
    widget: {
        widgetObjectsPrefix: "WidgetRenderer",
    },
};

export const initialPlayerPos: Triplet = [0, 10, 0]; // TODO - Move this inside a Player widget
